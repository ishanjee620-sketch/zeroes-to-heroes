CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT,
    email TEXT,
    age INTEGER
);

INSERT INTO users (id, name, email, age) VALUES
(1, 'Ishan', 'ishan@gmail.com', 21),
(2, 'Kartika', 'kartika@yahoo.com', 19),
(3, 'Virat', 'virat@gmail.com', 17);

SELECT * FROM users;

SELECT * FROM users
WHERE age >= 18;

UPDATE users
SET age = 18
WHERE name = 'Kartika';

DELETE FROM users
WHERE id = 1;

-- Bonus Challenge

SELECT * FROM users
ORDER BY age ASC;

SELECT * FROM users
WHERE email LIKE '%@gmail.com';

SELECT COUNT(*) FROM users;
